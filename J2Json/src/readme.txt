readme
J2Json 一个将java对象转换为js对象的工具
	目标1:实现java->json的转换----2015-06-09开始
		资源:json结构参考http://json.org/
		分目标1:实现将整个对象转换成json对象
			存在问题:数据类型转换问题(0); --完成
				集合类型和数组的转换(1); --完成
				map 对象的转换(1); --完成
				对象属性值为另一个对象的转换(0);--完成
				对象成员属性类型为自身的对象的转换(2); --不会存在,这种问题由程序员造成
				对象中静态属性的转换,和常量的转换(1);  --只支持通过get方法得到对象的属性值
				a.c->b b.d->a 的情况的转换(4);    --完成
		分目标2:实现将对象的部分属性自定义转换成json对象(16)
		j2json 未解决的问题 互相引用会出现死循环  无法自定义序列化属性  
	互相引用死循环问题
		将所有要序列化到json的对象放到一个集合中,如果该json化的对象在集合中中存在,则在json中直接放置该对象所指向的引用.
	无法自定义序列化属性
		黑名单\白名单模式:
			将要序列化的对象的属性以黑名单\白名单的形式来设置.
	性能优化(优化方式未知)
	
	开发者模式(未来计划)
	
	log4g的引用
	
	
	
	目标2:实现json->java的转换----开始日期未知
	
	